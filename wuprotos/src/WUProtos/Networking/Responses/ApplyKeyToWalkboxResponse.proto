syntax = "proto3";
package WUProtos.Networking.Responses;

message ApplyKeyToWalkboxResponse {
	Status status = 1;

	enum Status {
		UNKNOWN = 0;
		SUCCESS = 1;
		ERROR = 2;
		INVALID_WALKBOX = 3;
		KEY_UNAVAILABLE = 4;
		ALREADY_UNLOCKING = 5;
	}
}
